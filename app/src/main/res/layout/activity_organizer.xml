<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Main content: toolbar + fragment container -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="Organizer"
            app:navigationIcon="@drawable/ic_menu"/>

        <!-- Where screens (fragments) go -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/organizer_nav_host"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:defaultNavHost="true"
            android:layout_marginTop="?attr/actionBarSize"/>
<!--            app:navGraph="@navigation/organizer_nav_graph"-->

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- The drawer: header + menu items -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:headerLayout="@layout/nav_header_organizer"
        app:menu="@menu/menu_organizer_drawer"
        app:itemIconTint="@color/black" />
</androidx.drawerlayout.widget.DrawerLayout>